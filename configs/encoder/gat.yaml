# @package encoder
name: gat
_target_: g2t_aml.models.encoder.GATEncoder   # implemented in Phase 7
in_dim: null          # inferred from the feature spec at build time
hidden_dim: 256
out_dim: 512
num_layers: 3
heads: 4
dropout: 0.1
edge_dim: null
residual: true
readout: attention    # {mean, sum, max, attention}
